# InkMarkdown — Domain Glossary

> 仅收录跨文档/跨模块复用的领域术语。不含实现细节、API 清单或路线图。

## 公式与图表

用户可见总称：涵盖 LaTeX 数学公式与 Mermaid 图表两类本地生成内容。ExampleApp 入口文案统一使用本词。

## 生成内容

由公式、图表等内容在本地生成的图片，纳入统一图片管理；其来源不是网络图片地址。

## 开启态 / 关闭态

opt-in 对照。开启态：`latexRendering.isEnabled` / `mermaidRendering.isEnabled` 为真，公式与图表渲染为位图。关闭态：保持默认关闭，围栏/公式源码按普通 Markdown 展示，用于对照演示。

## 单块闭合即渲染

流式场景下，块级围栏（如 ` ```mermaid `）或块级公式（`$$` / `\[...\]`）一旦在源文本中闭合，该块立即进入异步生图，无需等待整条 SSE 流结束（`[DONE]`）。允许后文仍在吐字、前文已出图。

## 渲染会话

流式 Markdown 从接收分片开始，到取消、重置或终态块可交互为止的一次呈现生命周期。会话拥有该次呈现的内容真相和阶段语义；网络传输与应用业务状态不属于会话。

## 呈现周期

一份 Markdown 内容持续显示与更新的一段生命周期。静态内容由同一宿主呈现实例界定；流式内容由同一渲染会话界定，新文档、静态宿主重建、流式重置或取消会开启新周期。

## 逻辑块

在同一呈现周期内被可靠识别为同一语义单元的 Markdown 块。块内容可以增量变化；无法可靠确认延续关系时，按新块处理。

## 块呈现状态

不改变 Markdown 内容真相、但影响逻辑块可见形态或交互行为的状态，例如思考块的展开或折叠。该状态只在当前呈现周期内延续。

## 块呈现连续性

同一呈现周期内，同一逻辑 Markdown 块从增量内容到终态呈现时，其用户可见状态与交互状态保持连续。

## v0.0.2 语义对齐矩阵

v0.0.2 已承诺 Markdown 能力在 UIKit 富文本、UIKit 块、流式终态与 SwiftUI adapter 四条呈现通道中的一致性边界。明确记录的不支持项不属于本矩阵的缺陷。

_避免：完整语义、全部 Markdown 语义_

## 图片管理后端

为图片资源提供加载、解码、缓存、请求合并和取消能力的组件。宿主可以选择库提供的后端，也可以接入自身已有的图片管理模块。

## 图片生成器

将公式、图表等非图片内容转换为图片的组件。生成职责独立于图片管理；生成结果可交给统一的图片管理后端使用。

## 图片业务策略

宿主针对图片来源施加的产品规则，例如域名白名单或业务 URL 约束。库提供可选配置能力，但不默认替宿主制定这些规则。

## 图片资源安全边界

图片加载必须遵守的资源保护，包括协议、响应、图片数据、大小、重定向与取消约束；它不表达宿主的域名业务策略。来源入口由呈现侧校验，传输与解码限制由所选图片管理后端执行。

_避免：用“图片安全策略”同时指代业务规则与资源保护_

## 图片资源预算

宿主为一组图片加载与缓存分配的资源上限，包括缓存容量、并发加载数和待处理请求数。预算约束资源占用，不表达图片来源的业务允许范围。
