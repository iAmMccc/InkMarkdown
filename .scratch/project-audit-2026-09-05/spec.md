# 当前项目审核与根因修复

> 2026-09-06：用户明确确认“是的，请按照报告实施”，包括推荐的 Store 所有权方案；进入实施与验证阶段，无需再次请求同一方案的批准。

## 已确认范围

- 审核当前工作树中的全部模块，修复现有能力的缺陷；尚未承诺的新功能单独列为建议。
- 用户于 2026-09-05 明确，本轮不需要验收 iOS 15，优先保证 iOS 18 与 iOS 26。该指令调整验收矩阵，不修改现有 iOS 15 deployment target。
- 保持 UIKit rendering engine 与独立 SwiftUI adapter 的架构边界，沿用已有领域术语和已确认 ADR。
- 在共享职责边界修复根因；不得添加仅针对单一症状的条件分支。保留已发布 0.0.1 API 的源兼容性。
- 子代理使用独立任务说明，不继承主代理上下文。
- 保留进入任务时已有的未提交改动；本任务不包含 commit、push 或发布。

## 审核基线

- 仓库：`/Users/shizihan/DailyUse/Github/InkMarkdown`。
- 分支：`feat/swiftUI`。
- HEAD：`46cb52f90ddc0d3648d07a8913f1a9ca24af2b02`。
- 审核对象包含未提交改动，不等同于该 HEAD 的干净快照。
- 本地可用验收 runtime：iOS 18.5、iOS 26.5。
- 历史 343 项测试通过不作为本轮验证结论。

## 执行顺序

1. 核对当前源码、文档、调用链及工程验证能力，记录事实与待验证假设。
2. 将确认缺陷整理为根因、影响范围、修复方案和回归条件，交由用户确认共同理解。
3. 在确认方案后实施修复，添加必要的关键路径回归测试。
4. 使用 XcodeBuildMCP 在 iOS 18.5 与 iOS 26.5 验证；分别记录包测试、ExampleApp 构建及适用的交互证据。
5. 更新审核记录与当前状态，明确已修复项、残余缺口和未执行的验收。

## 验收条件

- 每个修复有真实触发场景、源码证据和适当的回归验证。
- 共享渲染语义、流式生命周期、配置重配及图片/addon 的资源边界保持一致。
- 不以编译通过代替交互验收，不以子代理完成代替工程验证。
- 本轮 iOS 15 不验收；不将此项计作本轮未完成工作，也不宣称已有最低版本运行证据。
- 审核报告如存在未覆盖区域，应明确列出，不宣称无剩余缺陷。

## 执行结果（2026-09-06）

F01–F17 已修复。iOS 18.5 与 iOS 26.5 各通过 361 项 Package 测试和 8 项 ExampleApp hosted tests；ExampleApp Release 构建通过。两系统完成启动和 Thought 状态交互检查，iOS 18.5 另有旋转与 Mermaid 宽图可见性证据。临时本地依赖覆盖已全部恢复，未提交或推送。

iOS 18.5 Mermaid 测试曾出现 WebProcess 超时，恢复原逻辑后的最终全量通过；该 detached WebKit 生命周期风险与其他未验收矩阵在审核报告中明确保留，不宣称已消除。
